\section{问题一的模型建立与求解}

\subsection{数据预处理}

采用 Z-score 标准化处理：$x' = \dfrac{x - \mu}{\sigma}$。

\subsection{模型建立}

采用多元线性回归作为基础模型：
\[
  y = \theta_0 + \theta_1 x_1 + \theta_2 x_2 + \dots + \theta_n x_n + \epsilon
\]

同时引入随机森林模型进行对比分析，通过集成多棵决策树提高精度和鲁棒性。

\subsection{求解结果}

随机森林的测试集 $R^2$ 达到 0.896，优于线性回归的 0.834。

\subsection{问题小结}

% 每个问题章节必须以「问题小结」收尾，写清三段式：
% 本题做了什么 → 得到什么 → 对下一题的支撑（最后一题写对全文结论的支撑）。
% 例：本题建立了多元线性回归与随机森林对比模型，得到最优模型 $R^2=0.896$；
% 其中筛选出的关键特征将作为下一题模型输入，支撑后续的优化与预测。
